- content_for :head do
  = stylesheet_link_tag 'partials/console'

.console_tabs
  = device_consoles @dispositivos_reservados


.text_area
  = text_area(:irc, :area, :cols => 50, :rows => 10)
  = #form_tag message_path, :remote => true, :id => 'chat' do
.text_field
  = label_tag(:message, "Mensaje:")
  = text_field_tag(:message)
.submit
  = submit_tag("Enviar")