- if free_devices.empty?
  %div{:style => 'color: #ff0000'}
    El horario seleccionado no tiene dispositivos disponibles
- else
  - free_devices.each do |dispositivo|
    %div
      = check_box_tag "practica[dispositivo_ids][]", dispositivo.id
      = dispositivo.nombre