chat_terminal = chat_terminal;
if(username != '<%= @mensaje[:user] %>')
{
    chat_terminal.echo('<%= @mensaje[:user] %> > <%= escape_javascript @mensaje[:message].html_safe %>').css('color','blue');
}