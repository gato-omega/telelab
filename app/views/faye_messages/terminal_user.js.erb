terminals = terminals;
if(username != '<%= @mensaje[:user] %>')
{
    terminals['<%= @terminal_id %>'].echo('<%= @mensaje[:user] %> > <%= escape_javascript @mensaje[:message].html_safe %>').css("color", "white");
}