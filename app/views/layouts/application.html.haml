!!!
%html
  %head
    %title= content_for?(:title) ? yield(:title) : "Untitled"
    = stylesheet_link_tag "token-input-facebook"
    = stylesheet_link_tag :jquery_ui
    = stylesheet_link_tag "application"
    
    = javascript_include_tag :defaults, "jquery"
    = javascript_include_tag :jquery_tokeninput
    = javascript_include_tag :jquery_ui

    -#/Set by the compass instalation, the fist line change the letters style to Times New Roman
    -#= stylesheet_link_tag 'screen.css', :media => 'screen, projection'
    -#= stylesheet_link_tag 'print.css', :media => 'print'
    -#/[if IE]
    -#= stylesheet_link_tag 'ie.css', :media => 'screen, projection'


    = csrf_meta_tag
    = yield(:head)
  %body
    #container
      %h1 APPLICATION LAYOUT APLICATION
      %h2 Laboratorio remote
      - flash.each do |name, msg|
        = ui_flash name, msg
      %p.notice= notice
      %p.alert= alert
      = content_tag :h1, yield(:title) if show_title?
      #user_nav
        - if user_signed_in?
          Signed in as
          = succeed "," do
            %strong= current_user.username
          you are a
          %strong= current_user.type
          \. Not you?
          \#{link_to "Sign out", destroy_user_session_path} | #{link_to "Profile", profile_edit_path(current_user.username), :as => 'User'}
        - else
          Please #{link_to "Sign in", new_user_session_path} to have more privileges
      = yield
      %p
