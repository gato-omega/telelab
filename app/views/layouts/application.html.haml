!!!
%html
  %head
    %title= content_for?(:title) ? yield(:title) : "Untitled"
    = stylesheet_link_tag :defaults
    
    = javascript_include_tag :defaults
    = javascript_include_tag :jquery_tokeninput
    = javascript_include_tag :jquery_datatables

    = csrf_meta_tag
    = yield(:head)
  %body
    %header
      .central
        .session_info
          - if user_signed_in?
            Loggeado como
            %strong= current_user.username
            , tienes privilegios de
            %strong= current_user.type
            No eres tu?
            = link_to "Cerrar sesion", destroy_user_session_path
            |
            = link_to "Perfil", profile_edit_path(current_user.username), :as => 'User'
            |
          - else
            Please
            = link_to "Sign in", new_user_session_path
            to have more privileges
            |
          = link_to "Ayuda", 'http://www.google.com'

    #left_nav
      = yield(:leftnav)
    %section
      - flash.each do |name, msg|
        = ui_flash name, msg
      %p.notice= notice
      %p.alert= alert
      = content_tag :h1, yield(:title) if show_title?

      = yield
      %p

    %footer
      .central
        Copyright © 2011

