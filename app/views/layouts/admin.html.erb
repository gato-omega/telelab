<!DOCTYPE html>
<html>
<head>
  <title><%= content_for?(:title) ? yield(:title) : "Untitled" %></title>
  <%= stylesheet_link_tag "application" %>
  <%= stylesheet_link_tag "jquery-ui-1.8.13.custom" %>

  <%= javascript_include_tag :defaults %>
  <%= javascript_include_tag :jquery_ui %>

  <%= csrf_meta_tag %>

  <%= yield(:head) %>

</head>
<body>
<div id="container">

  <h1>CAMBIA ALGO.... METELE LA MANO... COMO AL ANDROID!!</h1>

  <h2>Laboratorio remote</h2>


  <% flash.each do |name, msg| %>
    <%= ui_flash name, msg %>
  <% end %>

  <p class="notice"><%= notice %></p>
  <p class="alert"><%= alert %></p>
  
  <%= content_tag :h1, yield(:title) if show_title? %>

  <div id="user_nav">
    <% if user_signed_in? %>
        Signed in as <strong><%= current_user.username %></strong>, you are a <strong><%= current_user.type %></strong>
        . Not you?
        <%= link_to "Sign out", destroy_user_session_path %>
        | <%= link_to "Profile", profile_edit_path(current_user.username), :as => 'User' %>
    <% else %>
        Please <%= link_to "Sign in", new_user_session_path %> to have more privileges
    <% end %>
  </div>

  <%= yield %>

  <p>
    <%= link_to "Manage Teachers", teachers_path %>
    <br/>
    <%= link_to "Manage Cursos", courses_path %>
    <br/>
    <%= link_to "Manage Users", users_path %>
  </p>

</div>
</body>
</html>